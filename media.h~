#ifndef MEDIA
#define MEDIA

#include <iostream>
#include <cstring>

using namespace std;

class Media{
 public:
  Media();
  char* title;
  int year;
  Media (char*title,int year);
  virtual char* getTitle();
  virtual int getYear();
  virtual void printMedia();
};

#endif
